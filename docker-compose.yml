services:
  dct-mcp-server:
    build: .
    image: dct-mcp-server:local
    stdin_open: true
    tty: false
    # Copy .env.example to .env and fill in DCT_API_KEY and DCT_BASE_URL before running
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
