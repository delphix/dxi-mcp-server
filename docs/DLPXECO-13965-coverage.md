# Spec-Code Coverage: DLPXECO-13965

| FR-ID | Description | Status | Evidence (file:line) |
|-------|-------------|--------|----------------------|
| FR-001 | `vdbIds` parameter — list of VDB IDs, validated non-empty, list type, all string elements | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:2989` — `return {"error": "vdbIds must be a non-empty list of VDB IDs"}` (empty list check); `:3005` (string type check); `tests/dlpxeco-13965-test.py:229` — `test_bulk_start_empty_list_returns_error`; `:454` — `test_bulk_start_vdbIds_string_returns_validation_error`; `:460` — `test_bulk_start_vdbIds_list_with_none_element_returns_validation_error` |
| FR-002 | Concurrent fan-out using `asyncio.Semaphore` capped by `DCT_BULK_CONCURRENCY` (default 5, max 50) | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:117` — `raw = os.environ.get("DCT_BULK_CONCURRENCY")`; `:178` — `Async batch coroutine that fans out per-VDB DCT calls under one Semaphore`; `tests/dlpxeco-13965-test.py:277` — `with patch.dict(os.environ, {"DCT_BULK_CONCURRENCY": "3"}):`; `:285` — `assert max_in_flight <= 3` |
| FR-003 | Per-VDB failure isolation — partial_success when some fail, failed when all fail | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:219` — `status = "failed"`; `:221` — `status = "partial_success"`; `tests/dlpxeco-13965-test.py:167` — `assert result["status"] == "partial_success"`; `:203` — `assert result["status"] == "failed"` |
| FR-004 | Structured response with 5-key shape: status, total, succeeded, failed, jobs | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:224` — `"status": status`; `:225` — `"total": len(vdb_ids)`; `tests/dlpxeco-13965-test.py:124` — `assert set(result.keys()) == {"status", "total", "succeeded", "failed", "jobs"}` |
| FR-005 | Confirmation gate for bulk_stop and bulk_disable when count >= 5 (threshold) | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:57` — `"status": "confirmation_required"`; `:154` — `"status": "confirmation_required"`; `tests/dlpxeco-13965-test.py:308` — `assert result.get("status") == "confirmation_required"` (bulk_stop 6); `:389` — `assert result.get("status") == "confirmation_required"` (bulk_disable 6); `:366` — no gate with 3 VDBs (SC6) |
| FR-006 | Bulk actions visible in self_service (vdb_tool) and continuous_data_admin (data_tool); absent from reporting_insights | PASS | `src/dct_mcp_server/config/toolsets/self_service.txt:27` — `POST\|/vdbs/bulk_start\|bulk_start`; `:28` — `bulk_stop`; `:29` — `bulk_enable`; `:30` — `bulk_disable`; `tests/dlpxeco-13965-test.py:613` — `test_bulk_visibility_in_self_service_and_cda_only`; `:697` — `test_reporting_insights_excludes_bulk_actions` |
| FR-007 | Structured logging: INFO on dispatch and completion; DEBUG per-VDB with outcome=success/failure | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:193` — `logger.debug(f"bulk action={action} vdbId={vid} outcome=success")`; `:198` — `outcome=failure error={err}`; `:2991` — `logger.info(f"bulk action={action} total=...")`; `tests/dlpxeco-13965-test.py:499` — `assert len(info_records) >= 2`; `:526` — `assert len(debug_success_records) == 3`; `:556` — `assert len(debug_failure_records) == 3` |
| FR-008 | Backward compatibility — existing single-VDB actions (start_vdb, stop_vdb, etc.) unchanged | PASS | `src/dct_mcp_server/tools/dataset_endpoints_tool.py:818` — `ACTION: start_vdb`; `tests/dlpxeco-13965-test.py:634` — `test_single_vdb_start_unchanged_after_bulk_addition`; `:686` — `assert result == expected_payload` |
